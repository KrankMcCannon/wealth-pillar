---
phase: 03-input-optimization
plan: 03
type: execute
wave: 2
depends_on: ['01', '02']
files_modified: ['src/components/form/form-currency-input.tsx']
autonomous: false
requirements: [3.2, 3.3]
must_haves:
  truths:
    - 'FormCurrencyInput triggers numeric decimal keyboard on mobile'
    - 'All inputs follow consistent Tailwind v4 styling'
  artifacts:
    - path: 'src/components/form/form-currency-input.tsx'
      provides: 'Optimized currency input'
---

<objective>
Optimize the FormCurrencyInput and perform final phase verification across all modernized inputs.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Optimize FormCurrencyInput</name>
  <files>src/components/form/form-currency-input.tsx</files>
  <action>
    - Ensure `inputMode="decimal"` is correctly set for mobile keyboards.
    - Update component styling to match the heights and borders of the new Date and Select fields.
    - Verify decimal normalization logic on blur.
  </action>
  <verify>Test input on mobile device/emulator; confirm numeric keyboard appears.</verify>
  <done>Currency input is optimized.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Final Phase Verification</name>
  <how-to-verify>
    1. Open a form containing all three input types (e.g., TransactionForm).
    2. Verify DateField: Popover (D), Drawer (M), Italian locale.
    3. Verify Category/FormSelect: Command search (D), Drawer (M).
    4. Verify CurrencyInput: Styling consistency, mobile keyboard.
    5. Test keyboard navigation (Tab/Enter) and touch targets.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>
</tasks>

<verification>
Manual verification of the end-to-end form experience.
</verification>

<success_criteria>
The entire form input experience is optimized for modern UX/UI.
</success_criteria>

<output>
After completion, create '.planning/phases/03-input-optimization/03-03-SUMMARY.md'
</output>
