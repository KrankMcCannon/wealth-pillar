/**
 * Segment paths (senza prefisso locale — coerente con App Router + next-intl).
 */
export const APP_ROUTE = {
  transactions: '/transactions',
  accounts: '/accounts',
  home: '/home',
  budgets: '/budgets',
  reports: '/reports',
  investments: '/investments',
} as const;
