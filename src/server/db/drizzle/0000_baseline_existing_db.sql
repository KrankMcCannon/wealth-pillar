-- Baseline marker: production schema was applied via Supabase migrations before Drizzle journal adoption.
-- Safe to run only on empty databases. Existing Wealth Pillar DBs should skip this file.
SELECT 1;
