Here is Claude's plan:
╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌
 UI Component Reorganization & Refactoring Plan

 Goal: Comprehensive UI modernization following atomic design principles with pattern extraction and improved component organization.

 Approach: Fresh comprehensive plan incorporating existing refactoring progress + new improvements.

 Estimated Time: 4-5 hours
 Code Reduction: ~900-1100 lines
 Files Affected: 25-30 files

 ---
 Current State Analysis

 ✅ Already Completed (from feature/ui-refactor branch)

 - useUserFilter hook - shared state management (5+ pages)
 - PageContainer, PageHeaderWithBack, BackButton components
 - SkeletonBox, SkeletonList primitives
 - StickyHeader, SectionHeader layout components
 - Relocated: PermissionGuard → shared/, password components → ui/

 🔄 In Progress

 - Content pages partially refactored (dashboard, budgets)
 - Skeleton components partially using SkeletonList
 - Git status shows pending changes in accounts, investments, transactions

 ❌ Missing Patterns (HIGH ROI - ~700 lines reduction)

 1. EmptyState component - duplicated in transactions, investments, budgets
 2. useFormModal hook - modal state in transactions (4×), budgets (4×), accounts (2×)
 3. useDeleteConfirmation hook - delete flows in transactions, budgets, settings
 4. useMapFromArray hook - array-to-map transformations in transactions, budgets
 5. DashboardPageLayout component - repeated structure across 7 content files

 📁 Directory Structure Issues

 - ui/primitives/date-drawer/ too deeply nested (should be ui/date-picker/)
 - Password components scattered in ui/ (should be ui/auth/)
 - RoleBadge in ui/ but PermissionGuard in shared/ (group together)
 - Hooks not organized by category (will scale poorly)

 ---
 Implementation Plan

 Phase 1: Prepare & Verify (10 min)

 Goal: Establish baseline and create safety checkpoint.

 Steps:
 1. Run TypeScript check: npx tsc --noEmit
 2. Create git checkpoint: git add -A && git commit -m "checkpoint: before comprehensive UI refactor"
 3. Document current git status for rollback

 Verification: Clean TypeScript build, clean git status

 ---
 Phase 2: Extract High-Priority Hooks (45 min)

 Goal: Create reusable hooks to eliminate duplicated state management.

 2.1 Create useFormModal Hook (15 min)

 NEW: /src/hooks/state/use-form-modal.ts

 "use client";
 import { useState, useCallback } from "react";

 export function useFormModal<T = any>() {
   const [isOpen, setIsOpen] = useState(false);
   const [mode, setMode] = useState<'create' | 'edit'>('create');
   const [entity, setEntity] = useState<T | undefined>();

   const openCreate = useCallback(() => {
     setMode('create');
     setEntity(undefined);
     setIsOpen(true);
   }, []);

   const openEdit = useCallback((item: T) => {
     setMode('edit');
     setEntity(item);
     setIsOpen(true);
   }, []);

   const close = useCallback(() => {
     setIsOpen(false);
     setEntity(undefined);
   }, []);

   return {
     isOpen,
     mode,
     entity,
     openCreate,
     openEdit,
     close,
     setIsOpen, // For manual control
   };
 }

 Impact: 10+ instances in transactions, budgets, accounts, dashboard

 2.2 Create useDeleteConfirmation Hook (15 min)

 NEW: /src/hooks/state/use-delete-confirmation.ts

 "use client";
 import { useState, useCallback } from "react";

 export function useDeleteConfirmation<T = any>() {
   const [isOpen, setIsOpen] = useState(false);
   const [itemToDelete, setItemToDelete] = useState<T | null>(null);
   const [isDeleting, setIsDeleting] = useState(false);

   const openDialog = useCallback((item: T) => {
     setItemToDelete(item);
     setIsOpen(true);
   }, []);

   const closeDialog = useCallback(() => {
     setIsOpen(false);
     setItemToDelete(null);
     setIsDeleting(false);
   }, []);

   const executeDelete = useCallback(
     async (deleteAction: (item: T) => Promise<void>) => {
       if (!itemToDelete) return;

       setIsDeleting(true);
       try {
         await deleteAction(itemToDelete);
         closeDialog();
       } catch (error) {
         setIsDeleting(false);
         throw error;
       }
     },
     [itemToDelete, closeDialog]
   );

   return {
     isOpen,
     itemToDelete,
     isDeleting,
     openDialog,
     closeDialog,
     executeDelete,
   };
 }

 Impact: 6+ instances in transactions, budgets, settings

 2.3 Create useMapFromArray Hook (10 min)

 NEW: /src/hooks/data/use-map-from-array.ts

 "use client";
 import { useMemo } from "react";

 export function useMapFromArray<T>(
   array: T[],
   keyField: keyof T,
   valueField: keyof T
 ): Record<string, any> {
   return useMemo(() => {
     const map: Record<string, any> = {};
     array.forEach((item) => {
       const key = String(item[keyField]);
       map[key] = item[valueField];
     });
     return map;
   }, [array, keyField, valueField]);
 }

 // Convenience version for id -> name mapping
 export function useIdNameMap<T extends { id: string; name: string }>(
   array: T[]
 ): Record<string, string> {
   return useMapFromArray(array, 'id', 'name');
 }

 Impact: 4+ instances in transactions, budgets

 2.4 Create Barrel Exports (5 min)

 NEW: /src/hooks/state/index.ts
 export * from "./use-form-modal";
 export * from "./use-delete-confirmation";

 NEW: /src/hooks/data/index.ts
 export * from "./use-map-from-array";

 MODIFY: /src/hooks/index.ts
 export * from "./use-user-filter";
 export * from "./state";
 export * from "./data";
 // ... existing exports

 Verification: TypeScript compiles, all hooks exportable
 Commit: git commit -m "feat: add useFormModal, useDeleteConfirmation, and useMapFromArray hooks"

 ---
 Phase 3: Create Missing Components (30 min)

 Goal: Build reusable components for duplicated UI patterns.

 3.1 Create EmptyState Component (15 min)

 NEW: /src/components/shared/empty-state.tsx

 "use client";
 import { cn } from "@/lib";
 import { LucideIcon } from "lucide-react";

 interface EmptyStateProps {
   icon?: LucideIcon;
   title: string;
   description?: string;
   action?: React.ReactNode;
   className?: string;
 }

 export function EmptyState({
   icon: Icon,
   title,
   description,
   action,
   className
 }: EmptyStateProps) {
   return (
     <div className={cn("py-12 text-center", className)}>
       {Icon && (
         <div className="flex justify-center mb-4">
           <Icon className="h-12 w-12 text-muted-foreground" />
         </div>
       )}
       <h3 className="text-lg font-semibold text-foreground mb-2">{title}</h3>
       {description && (
         <p className="text-sm text-muted-foreground mb-4 max-w-md mx-auto">
           {description}
         </p>
       )}
       {action && <div className="mt-6">{action}</div>}
     </div>
   );
 }

 MODIFY: /src/components/shared/index.ts
 export * from "./empty-state";
 // ... existing exports

 Impact: 4+ instances across transactions, investments, budgets

 3.2 Create DashboardPageLayout Component (15 min)

 NEW: /src/components/layout/dashboard-page-layout.tsx

 "use client";
 import { PageContainer } from "./page-container";
 import { BottomNavigation } from "./bottom-navigation";
 import { cn } from "@/lib";

 interface DashboardPageLayoutProps {
   header?: React.ReactNode;
   userSelector?: React.ReactNode;
   tabs?: React.ReactNode;
   children: React.ReactNode;
   mainClassName?: string;
 }

 export function DashboardPageLayout({
   header,
   userSelector,
   tabs,
   children,
   mainClassName
 }: DashboardPageLayoutProps) {
   return (
     <PageContainer>
       <div className="flex-1">
         {header}
         {userSelector}
         {tabs}
         <main className={cn("p-4 pb-24", mainClassName)}>
           {children}
         </main>
       </div>
       <BottomNavigation />
     </PageContainer>
   );
 }

 MODIFY: /src/components/layout/index.ts
 export * from "./dashboard-page-layout";
 // ... existing exports

 Impact: 7 content files (dashboard, budgets, transactions, accounts, investments, reports, settings)

 Verification: TypeScript compiles
 Commit: git commit -m "feat: add EmptyState and DashboardPageLayout components"

 ---
 Phase 4: Refactor Content Pages (60 min)

 Goal: Update all content pages to use new hooks and components.

 Priority Order (based on complexity):

 1. transactions-content.tsx (20 min) - Most complex, all patterns
 2. budgets-content.tsx (15 min) - Multiple modals, delete flows
 3. accounts-content.tsx (10 min) - Form modals, layout
 4. investments-content.tsx (5 min) - Simple empty state
 5. reports-content.tsx (5 min) - Layout standardization
 6. settings-content.tsx (5 min) - Layout standardization

 Pattern for Each Page:

 BEFORE (transactions-content.tsx lines 64-71):
 const [isTransactionFormOpen, setIsTransactionFormOpen] = useState(false);
 const [isRecurringFormOpen, setIsRecurringFormOpen] = useState(false);
 const [formMode, setFormMode] = useState<'create' | 'edit'>('create');
 const [selectedTransaction, setSelectedTransaction] = useState<Transaction | undefined>();
 const [isDeleteConfirmOpen, setIsDeleteConfirmOpen] = useState(false);
 const [transactionToDelete, setTransactionToDelete] = useState<Transaction | null>(null);
 const [isDeleting, setIsDeleting] = useState(false);

 AFTER:
 const transactionModal = useFormModal<Transaction>();
 const recurringModal = useFormModal();
 const deleteConfirm = useDeleteConfirmation<Transaction>();
 const accountNames = useIdNameMap(accounts);

 Reduction: ~15-20 lines per page × 6 pages = ~100 lines

 Verification: Run TypeScript check after each page
 Commit: git commit -m "refactor: update all content pages to use new hooks and components"

 ---
 Phase 5: Reorganize Directory Structure (40 min)

 Goal: Improve component discoverability and organization.

 5.1 Organize Hooks by Category (10 min)

 Current:
 hooks/
 ├── use-user-filter.ts
 └── index.ts

 Target:
 hooks/
 ├── state/
 │   ├── use-user-filter.ts (MOVE)
 │   ├── use-form-modal.ts (ALREADY CREATED)
 │   ├── use-delete-confirmation.ts (ALREADY CREATED)
 │   └── index.ts
 ├── data/
 │   ├── use-map-from-array.ts (ALREADY CREATED)
 │   └── index.ts
 ├── dom/
 │   ├── use-local-storage.ts (if exists)
 │   ├── use-media-query.ts (if exists)
 │   └── index.ts
 └── index.ts (exports from all subdirs)

 Steps:
 1. Move use-user-filter.ts to state/
 2. Update imports in all files using useUserFilter
 3. Verify barrel exports chain correctly

 5.2 Elevate Date Drawer (10 min)

 MOVE: ui/primitives/date-drawer/ → ui/date-picker/

 Rationale: Complex feature component, not an atomic primitive

 Steps:
 1. Move directory: mv src/components/ui/primitives/date-drawer src/components/ui/date-picker
 2. Update imports in all files using date-picker components
 3. Update ui/index.ts to export from new location
 4. Remove from ui/primitives/index.ts

 5.3 Group Auth Components (10 min)

 MOVE to: ui/auth/
 - ui/password-input.tsx
 - ui/password-strength.tsx
 - ui/password-requirements.tsx

 Steps:
 1. Create ui/auth/ directory
 2. Move 3 password components
 3. Create ui/auth/index.ts barrel export
 4. Update ui/index.ts to export from ./auth
 5. Update imports in auth feature files

 5.4 Consolidate Permission Components (10 min)

 MOVE: ui/role-badge.tsx → shared/role-badge.tsx

 Rationale: App-wide concern, belongs with PermissionGuard

 Steps:
 1. Move file to shared/
 2. Update shared/index.ts
 3. Add re-export in ui/index.ts for backward compatibility (temporary)
 4. Update imports in features

 Verification: Run npx tsc --noEmit after all moves
 Commit: git commit -m "refactor: reorganize hooks and components for better discoverability"

 ---
 Phase 6: Delete Unused Components (20 min)

 Goal: Remove components identified in original plan as unused.

 Delete from src/components/:

 # Form components
 rm src/components/form/form-checkbox-group.tsx

 # Layout components
 rm src/components/layout/content-section.tsx
 rm src/components/layout/page-header.tsx
 rm src/components/layout/page-layout.tsx
 rm src/components/layout/sidebar.tsx

 # Shared components
 rm src/components/shared/theme-provider.tsx

 # UI components
 rm src/components/ui/avatar.tsx
 rm src/components/ui/filter-bar.tsx
 rm src/components/ui/filter-chip.tsx
 rm src/components/ui/period-selector.tsx
 rm src/components/ui/progress.tsx
 rm src/components/ui/scroll-area.tsx

 # UI/layout components
 rm src/components/ui/layout/empty-state.tsx
 rm src/components/ui/layout/list-item.tsx
 rm src/components/ui/layout/section.tsx

 Delete from src/features/:

 # Auth components
 rm src/features/auth/components/auth-guard.tsx
 rm src/features/auth/components/auth-method-toggle.tsx

 # Dashboard components
 rm src/features/dashboard/components/dashboard-grid.tsx
 rm src/features/dashboard/components/metric-card.tsx
 rm src/features/dashboard/components/stats-section.tsx

 # Recurring components
 rm src/features/recurring/components/recurring-execution-manager.tsx
 rm src/features/recurring/components/series-reconciliation-view.tsx

 Update Index Files: Remove corresponding exports from all affected index.ts files.

 Verification:
 1. TypeScript check should pass (components were unused)
 2. No broken imports

 Commit: git commit -m "chore: delete 22 unused components and update index files"

 ---
 Phase 7: Enhance PageHeaderWithBack (15 min)

 Goal: Standardize action dropdown pattern.

 MODIFY: /src/components/layout/page-header-with-back.tsx

 Add actionsMenu prop:

 interface PageHeaderWithBackProps {
   title: string;
   onBack?: () => void;
   actions?: React.ReactNode;
   actionsMenu?: Array<{
     label: string;
     icon?: LucideIcon;
     onClick: () => void;
   }>;
   variant?: "primary" | "secondary" | "light";
 }

 Render dropdown when actionsMenu provided:

 {actionsMenu ? (
   <DropdownMenu>
     <DropdownMenuTrigger asChild>
       <Button variant="ghost" size="sm">
         <MoreVertical className="h-5 w-5" />
       </Button>
     </DropdownMenuTrigger>
     <DropdownMenuContent align="end">
       {actionsMenu.map((item, idx) => (
         <DropdownMenuItem key={idx} onClick={item.onClick}>
           {item.icon && <item.icon className="mr-3 h-4 w-4" />}
           {item.label}
         </DropdownMenuItem>
       ))}
     </DropdownMenuContent>
   </DropdownMenu>
 ) : actions ? actions : <div className="w-10" />}

 Impact: Simplifies action menus in transactions, budgets headers

 Commit: git commit -m "feat: enhance PageHeaderWithBack with actionsMenu prop"

 ---
 Phase 8: Update Feature Headers (20 min)

 Goal: Standardize header components using new patterns.

 8.1 Update AccountHeader (10 min)

 MODIFY: /src/features/accounts/components/AccountHeader.tsx

 Use PageHeaderWithBack with actionsMenu prop instead of custom implementation.

 8.2 Update BudgetHeader (10 min)

 MODIFY: /src/features/budgets/components/BudgetHeader.tsx

 Use PageHeaderWithBack with actionsMenu prop for dropdown actions.

 Verification: Headers work correctly, dropdowns functional
 Commit: git commit -m "refactor: update feature headers to use PageHeaderWithBack"

 ---
 Phase 9: Standardize Empty States (15 min)

 Goal: Replace inline empty states with EmptyState component.

 Files to Update:
 1. transactions-content.tsx (lines 322-341)
 2. investments-content.tsx (lines 79-83)
 3. Any feature-specific empty state components

 BEFORE (transactions):
 <div className={styles.emptyState.container}>
   <div className={styles.emptyState.icon}>
     <svg>...</svg>
   </div>
   <h3>Nessuna Transazione</h3>
   <p>Message</p>
 </div>

 AFTER:
 <EmptyState
   icon={FileText}
   title="Nessuna Transazione"
   description="Message"
 />

 Commit: git commit -m "refactor: standardize empty states using EmptyState component"

 ---
 Phase 10: Finalize Skeleton Components (20 min)

 Goal: Complete skeleton refactoring to use SkeletonList consistently.

 Files to Update (15+ files):
 - features/accounts/components/account-skeletons.tsx
 - features/budgets/components/BudgetLoadingSkeletons.tsx
 - features/dashboard/components/dashboard-skeletons.tsx
 - features/transactions/components/transaction-skeletons.tsx
 - All other skeleton files

 Pattern:

 BEFORE:
 {Array.from({ length: 3 }).map((_, i) => (
   <div key={i} className="h-16 bg-slate-100 rounded-lg animate-pulse" />
 ))}

 AFTER:
 <SkeletonList count={3} height="h-16" variant="medium" />

 Commit: git commit -m "refactor: use SkeletonList component in all skeleton files"

 ---
 Phase 11: Final Verification & Testing (30 min)

 Steps:

 1. Full TypeScript Check
 npx tsc --noEmit
 2. Build Verification
 npm run build
 3. Manual Browser Testing
   - Dashboard page - verify layout, user selector, modals
   - Budgets page - verify budget selector, modals, delete flow
   - Transactions page - verify transaction list, modals, delete flow
   - Accounts page - verify account list, modals
   - Investments page - verify empty state
   - Reports page - verify layout
   - Settings page - verify layout
   - Auth pages - verify password inputs still work
 4. Git Status Check
 git status
 git diff --stat
 5. Create Final Commit
 git add -A
 git commit -m "feat: complete comprehensive UI component reorganization

 - Extracted 5 reusable patterns (hooks and components)
 - Reorganized component directory structure
 - Deleted 22 unused components
 - Refactored 6 content pages
 - Standardized headers, empty states, and skeletons
 - Reduced codebase by ~900-1100 lines
 - Improved component discoverability and maintainability"

 Verification Checklist:
 - ✅ TypeScript compiles without errors
 - ✅ Build succeeds
 - ✅ No console errors in browser
 - ✅ All pages render correctly
 - ✅ All modals open/close properly
 - ✅ Delete confirmations work
 - ✅ User filtering works across pages
 - ✅ Empty states display correctly
 - ✅ Loading skeletons animate properly

 ---
 Critical Files Reference

 Files to Create (9 new files):

 1. /src/hooks/state/use-form-modal.ts
 2. /src/hooks/state/use-delete-confirmation.ts
 3. /src/hooks/state/index.ts
 4. /src/hooks/data/use-map-from-array.ts
 5. /src/hooks/data/index.ts
 6. /src/components/shared/empty-state.tsx
 7. /src/components/layout/dashboard-page-layout.tsx
 8. /src/components/ui/auth/index.ts
 9. /src/components/ui/date-picker/ (moved directory)

 Files to Modify (15-20 files):

 Content Pages:
 1. app/(dashboard)/transactions/transactions-content.tsx
 2. app/(dashboard)/budgets/budgets-content.tsx
 3. app/(dashboard)/accounts/accounts-content.tsx
 4. app/(dashboard)/investments/investments-content.tsx
 5. app/(dashboard)/reports/reports-content.tsx
 6. app/(dashboard)/settings/settings-content.tsx

 Components:
 7. src/components/layout/page-header-with-back.tsx
 8. src/features/accounts/components/AccountHeader.tsx
 9. src/features/budgets/components/BudgetHeader.tsx

 Barrel Exports:
 10. src/hooks/index.ts
 11. src/components/shared/index.ts
 12. src/components/layout/index.ts
 13. src/components/ui/index.ts
 14. src/components/ui/primitives/index.ts

 Skeleton Files (~15 files):
 15-30. All feature skeleton components

 Files to Delete (22 files):

 Listed in Phase 6 - unused components from form/, layout/, shared/, ui/, features/

 Files to Move:

 1. ui/primitives/date-drawer/ → ui/date-picker/
 2. ui/password-*.tsx (3 files) → ui/auth/
 3. ui/role-badge.tsx → shared/role-badge.tsx
 4. hooks/use-user-filter.ts → hooks/state/use-user-filter.ts

 ---
 Success Metrics

 Code Quality

 - TypeScript compiles without errors
 - Build succeeds
 - No console errors in browser
 - All pages render correctly
 - All interactive features work (modals, deletes, filtering)

 Quantitative Improvements

 - 5 new reusable patterns extracted (3 hooks, 2 components)
 - ~900-1100 lines of code reduced
 - 22 unused components deleted
 - 6 content pages refactored
 - 15+ skeleton files standardized
 - 4 directory improvements (hooks/, date-picker, auth/, permissions)

 Qualitative Improvements

 - Consistent state management patterns across all pages
 - Better component discoverability through logical grouping
 - Reduced cognitive load (less duplicated code to maintain)
 - Scalable hook organization (state/, data/, dom/)
 - Clearer separation of concerns
 - Modern atomic design principles applied

 Maintainability

 - Centralized form modal logic
 - Centralized delete confirmation logic
 - Reusable empty state component
 - Consistent page layout structure
 - Better organized component library
 - Clear component hierarchy

 ---
 Rollback Strategy

 Each phase is independently committed. If issues occur:

 1. Identify problematic commit:
 git log --oneline
 2. Revert to previous phase:
 git reset --hard <commit-hash>
 3. Or revert specific commit:
 git revert <commit-hash>
 4. Review what went wrong and retry

 Safety checkpoints:
 - Phase 1: Initial checkpoint
 - Phase 2: Hooks created
 - Phase 3: Components created
 - Phase 4: Pages refactored
 - Phase 5: Structure reorganized
 - Phases 6-10: Individual improvements
 - Phase 11: Final verified state

 ---
 Architecture Alignment

 This plan follows modern frontend best practices:

 ✅ Atomic Design - Primitives → UI → Layout → Shared → Features
 ✅ Component Composition - Small, focused, reusable components
 ✅ Separation of Concerns - Logic (hooks) separated from presentation
 ✅ DRY Principle - Eliminate duplication through extraction
 ✅ Scalable Organization - Logical grouping that grows with codebase
 ✅ Type Safety - Full TypeScript support throughout
 ✅ Clean Dependencies - Unidirectional dependency graph maintained
 ✅ Barrel Exports - Clear public APIs for each module
 ✅ Progressive Enhancement - Server components where appropriate
 ✅ Convention Over Configuration - Consistent naming and structure

 ---
 Notes

 - All changes are incremental and reversible
 - No behavioral changes - only structural refactoring
 - Existing architecture patterns maintained
 - Backward compatibility ensured during migration
 - Path aliases (@/) used consistently
 - Feature isolation preserved
 - TypeScript checked after every major change

 Ready for execution!